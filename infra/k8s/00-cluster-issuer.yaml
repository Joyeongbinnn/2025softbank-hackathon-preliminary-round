apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # Let's Encrypt 공식 인증 서버
    server: https://acme-v02.api.letsencrypt.org/directory
    email: yo12399@naver.com   # ← 너의 이메일로 변경
    privateKeySecretRef:
      name: letsencrypt-prod-key
    solvers:
    - http01:
        ingress:
          class: nginx
